{
  "name": "s3-listening-room",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An anonymous, offline-capable listening room for S3",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "package": "npm run build && python3 scripts/package.py",
    "package:original": "npm run build && python3 scripts/package.py --audio-format original"
  },
  "dependencies": {
    "@fontsource-variable/dm-sans": "5.3.0",
    "@fontsource/instrument-serif": "5.3.0",
    "@tonejs/midi": "2.0.28"
  }
}
